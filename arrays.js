let numeros = [11, 65, 6, 4, 87]
let variavelSoma = 0
for (i = 0; i < numeros.length; i++) {
variavelSoma += numeros[i];
}
let media = alert(`A média dos números de sua array é igual a ${variavelSoma/i}`)